@page "/"
@using IndexedDbTutorial.Services;
@using IndexedDbTutorial.Services.Models;
@using Microsoft.JSInterop;
@inject IndexedDbApi IndexedDb;




<PageTitle>Index</PageTitle>

<MudText Typo="Typo.h3" GutterBottom="true">Hello, world!</MudText>
<MudText Class="mb-8">Welcome to your new app, powered by MudBlazor!</MudText>
<MudAlert Severity="Severity.Normal">You can find documentation and examples on our website here: <MudLink Href="https://mudblazor.com" Typo="Typo.body2" Color="Color.Inherit"><b>www.mudblazor.com</b></MudLink></MudAlert>

@code {
    
    protected override async Task OnInitializedAsync()
    {
        //List<IndexedDbColumn> columns = new List<IndexedDbColumn>
        //{
        var column = new IndexedDbColumn { Name = "color", Unique = false, Index = true };

       // };
        var result = await IndexedDb.ExecuteTutorial();
        //await IndexedDb.createDb("Car", column);
    }
}